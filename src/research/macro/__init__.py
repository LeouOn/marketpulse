"""Macro layer for the multi-asset research lab.

Populated by T11-T15 (regime definitions, macro indicators, regime
detection logic). T2 ships only this empty placeholder so downstream
modules can ``from src.research.macro import ...`` without an ImportError
during the W1-W2 wiring window.

The ``Regime`` enum lives in :mod:`src.research.macro.regimes` (T12).
"""

# Macro layer -- populated by T11-T15.
